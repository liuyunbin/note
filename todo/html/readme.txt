
# HTML
## 超文本的标记语言
* 超文本: 指超链接
* 标记: 各种标签的类型
* 标签的默认效果不重要, 语义才重要

## 整体分类
* 块级标签
    * 每个块级标签独占一行, 多个块级标签从上往下排列
    * 默认宽度: 占满这个父级标签
    * 默认高度: 根据内容显示
    * 可以通过 CSS 调整宽和高
    * 块元素内几乎可以写任何标签
* 行内标签
    * 多个行内标签共享一行, 一行排满后, 下一行继续
    * 默认宽度: 根据内容显示
    * 默认高度: 根据内容显示
    * 不能通过 CSS 调整长和宽
    * 行内标签一般可以写行内元素, 不能写块级标签
* 行内块标签
    * 多个块级标签共享一行, 一行排满后, 下一行继续
    * 默认宽度: 根据内容显示
    * 默认高度: 根据内容显示
    * 可以通过 CSS 调整长和宽
    * 行内标签一般可以写行内元素, 不能写块级标签
* 块级标签 行内标签 行内块标签 可以通过 CSS 进行转化
* h1-h6 不能嵌套自身
* p 标签内不能包含块级标签
* a 标签可以嵌套除自身外的任何标签, 包括块级标签

## 常用标签
### 超链接
* a 行内标签, 可以包含除a以外的任意标签, 包括块级标签
* 常用说明
    * href="..." ------------------- 跳转链接
    * href="#..." ------------------ 跳转到锚点, 使用 id 指明锚点
    * href="#" --------------------- 跳转到顶部
    * href="" ---------------------- 刷新界面
    * bref="js..." ----------------- 执行脚本
    * bref="tel:123456" ------------ 打电话
    * bref="nailto:123@gmail.com" -- 发邮件
    * bref="sms:10086" ------------- 发短信
    * target="_blank" -------------- 在新的标签页打开
    * target="_self" --------------- 在当前标签页打开, 默认
    * download --------------------- 下载, 添加值表明文件名称

### 图片
* img ----------- 行内块标签,
    * src ------- 指明路径
    * alt ------- 指明图片内容, 便于搜索引擎
    * height ---- 指明高, 像素
    * width ----- 指明宽, 像素
* jpg ----------- 有损压缩 不支持透明背景 不支持动态图
* png ----------- 无损压缩   支持透明背景 不支持动态图
* bmp ----------- 不压缩   不支持透明背景 不支持动态图 占用空间极大, 一般不用
* gif ----------- 支持简单的透明背景, 支持动态图
* webp ---------- 支持透明背景和动态图, 兼容性不太好 IE 不支持
* base64 -------- 图片 -> 文本

### 表格
* table --------------- 表格, 块标签
    * border = "1" ---- 单元格边框为 1 像素, 外部边框为 1  像素
    * border = "10" --- 单元格边框为 1 像素, 外部边框为 10 像素
    * width ----------- 表格宽度, 至少
    * height ---------- 表格高度, 至少, 头部和脚注高度不变
    * cellspacing ----- 单元格边框之间的缝隙, 不是合并单元格的边框
* caption ------------- 表格标题
* thead --------------- 表格头部
    * height ---------- 高度
    * align="left" ---- 水平对齐
    * valign="top" ---- 垂直对齐
* tbody --------------- 表格主体
    * height ---------- 高度, 可能会自动调整
    * align="left" ---- 水平对齐
    * valign="top" ---- 垂直对齐
* tfoot --------------- 表格脚注
    * height ---------- 高度
    * align="left" ---- 水平对齐
    * valign="top" ---- 垂直对齐
* tr ------------------ 行
    * height ---------- 高度
    * align="left" ---- 水平对齐
    * valign="top" ---- 垂直对齐
* th ------------------ 列, 在 thead 中使用
    * width ----------- 单元格宽度, 会影响当前列的所以元素
    * height ---------- 单元格高度, 会影响当前行的所以元素
    * align="left" ---- 水平对齐
    * valign="top" ---- 垂直对齐
    * colspan --------- 跨行
    * rowspan --------- 跨列
* td ------------------ 列, 在 tbody 和 tfoot 中使用
    同 th

### 表单
* form -------------------- 表单
    * action -------------- 服务器地址
    * method -------------- get post
    * target -------------- 响应打开的位置
    * novalidate ---------- 表单提交时不再验证
* input ------------------- 输入, 行内元素
    * type ---------------- 类型
        * text ------------ 文本
        * password -------- 密码
        * radio ----------- 单选
        * checkbox -------- 复选
        * hidden ---------- 隐藏
        * submit ---------- 提交, 不能添加 name 选项, value 的值为显示的值
        * reset ----------- 重置, 不能添加 name 选项, value 的值为显示的值
        * button ---------- 普通按钮, 不能添加 name 选项, value 的值为显示的值
        * email ----------- 邮箱, 表单提交时会验证格式, 输入为空则不验证格式
        * url ------------- url,  表单提交时会验证格式, 输入为空则不验证格式
        * number ---------- 数字, 表单提交时会验证格式, 输入为空则不验证格式
        * search ---------- 搜索, 表单提交时不会验证格式
        * tel ------------- 电话, 表单提交时不会验证格式, 在移动端，会唤起数字键盘
        * range ----------- 范围选择, 默认值为 50, 表单提交时不会验证格式
        * color ----------- 颜色选择, 默认值为黑色, 表单提交时不会验证格式
        * date ------------ 日期选择, 默认值为空, 表单提交时不会验证格式
        * month ----------- 月份选择, 默认值为空, 表单提交时不会验证格式
        * week ------------ 周选择, 默认值为空, 表单提交时不会验证格式
        * time ------------ 时间选择框, 默认值为空, 表单提交时不会验证格式
        * datetime-local -- 日期+时间选择框, 默认值为空, 表单提交时不会验证格式
    * name ------------ 送入服务器的 key, 相同的name划分为一组
    * value ----------- 送入服务器的值
    * maxlength ------- 最大长度
    * checked --------- 对于单选为默认选项, 对于复选为必选项
    * disabled -------- 不能修改
    * placeholder ----- 提示文字, 不需要手动删除
    * required -------- 该输入项必填
    * autofocus ------- 进入网页自动获取焦点
    * autocomplete ---- 之前输入过的可以自动完成, 密码输入框和多行输入框不可用
    * pattern --------- 模式匹配
* button -------------- 不能添加 name 选项
    * type ------------ 普通按钮, 提交, 重置, 默认提交
    * disabled -------- 不能修改
* textarea ------------ 文本域, 能换行
    * name ------------ 送入服务器的key
    * rows ------------ 行
    * cols ------------ 列
    * disabled -------- 不能修改
* select -------------- 下拉框
    * name ------------ 送入服务器的key
    * disabled -------- 不能修改
    * option ---------- 选项
        * disabled ---- 不能修改
        * value ------- 属性, 送入服务器的值
        * selected ---- 默认选项
* label --------------- 获取焦点, 点击文件, 光标进入输入框
    * for ------------- 和 ID 关联
    * 建议用 label 包含
* fieldset ------------ 分组
    * legend ---------- 名称

### 框架
* iframe ----------- 嵌入, 行内块标签, 用于嵌入其他的数据, 比如, 网站
    * name --------- 框架的名称, 配合 target 使用
    * width -------- 框
    * height ------- 高
    * src ---------- 源
    * frameborder -- 是否显示边框

### 列表
* ol -------- 有序列表
* ul -------- 无序列表
* dl -------- 自定义列表
* li -------- 列表项, 位于有序列表和无序列表中
* dt -------- 术语名称, 位于自定义列表
* dd -------- 术语描述, 位于自定义列表
* datalist -- 搜索框的关键字提示, 配合 input
* details --- 用于展示问题和答案，或对专有名词进行解释
* summary --- 写在 details 的里面，用于指定问题或专有名词

### 视音频标签
* video ---------- 视频
* audio ---------- 音频
* src ------------ 视音频地址
* width ---------- 宽度
* height --------- 高度
* controls ------- 向用户显示控件, 比如播放 暂停按钮
* muted ---------- 静音
* autoplay ------- 自动播放
* loop ----------- 循环播放
* poster --------- 封面
* preload -------- 预加载, 如果使用 autoplay, 则忽略该属性
    * none ------- 不预加载
    * metadata --- 仅预先获取元数据, 例如长度
    * auto ------- 可以下载整个视音频

### 排版标签
* h1 - h6 -- 标题, 不能相互嵌套
* p -------- 段落, 内部不能写块标签
* div ------ 无特殊意义
* header --- 某区域头部
* footer --- 某区域末尾
* nav ------ 导航
* article -- 整体, 比如, 文章, 评论等等 -- 不常用
* section -- 整体中的某段 --------------- 不常用
* aside ---- 侧边栏

### 状态标签
* meter -------- 表示电量, 磁盘用量等
    * max ------ 最大值
    * min ------ 最小值
    * high ----- 用于划定区间
    * low ------ 用于划定区间
    * optimum -- 最优值, 该值所在的区间为最有区间, 默认绿色
    * value ---- 当前值
* progress ----- 表示进度条
    * max ------ 最大值
    * value ---- 当前值

### 常用文本标签
* em ------- 文本标签, 行内元素, 斜体
* strong --- 文本标签, 行内元素, 加粗
* span ----- 文本标签, 行内元素, 无特殊意义
* ruby ----- 注音
* rt ------- 注音, 放在 ruby 里
* mark ----- 关键字

### 其他常用标签
* br ------- 换行
* hr ------- 分割
* pre ------ 按原文显示

## 全局属性
* id ---------------- 本网页唯一, 配合 js 和 CSS 使用
* class ------------- 分类, 配合 css 使用
* style ------------- 样式
* dir --------------- 文字位于屏幕靠左还是靠右
* title ------------- 标题
* lang -------------- 语言
* contenteditable --- 是否可被用户编辑
* draggable --------- 是否可以被拖动
* hidden ------------ 隐藏元素
* spellcheck -------- 是否对元素进行拼写和语法检查
* contextmenu ------- 规定元素的上下文菜单，在用户鼠标右键点击元素时显示
* data-* ------------ 用于存储页面的私有定制数据

# CSS
## 选择器
* 通配选择器: 主要用于清除样式
* 元素选择器: `a`
* 类选择器: `.class_name`, 类可以重复
* ID选择器: `#id_name`, ID 不能重复
* 交集选择器: `a.class_name`, 且, 一般不用 ID, 标签最多一个, 类目一般只有一个
* 并集选择器: `a,.class_name`, 或, 可以包含多个 元素 类 ID 选择器
* 后代选择器: `a .class_name`
* 子代选择器: `a>.class_name`, 子代所有后代的一种特例
* 相邻兄弟选择器: `a+.class_name`
* 通用兄弟选择器: `a~.class_name` 所有兄弟
* 属性选择器
    1. `[属性名]` ----------- 选中具有某个属性的元素
    2. `[属性名="值"]` ------ 选中包含某个属性, 且属性值等于指定值的元素
    3. `[属性名^="值"]` ----- 选中包含某个属性, 且属性值以指定的值开头的元素
    4. `[属性名$="值"]` ----- 选中包含某个属性, 且属性值以指定的值结尾的元素
    5. `[属性名*=“值”]` ----- 选择包含某个属性, 属性值包含指定值的元素
* 伪类选择器 -- 动态伪类
    1. :link ------ 超链接未被访问的状态
    2. :visited --- 超链接访问过的状态
    3. :hover ----- 鼠标悬停在元素上的状态
    4. :active ---- 元素激活的状态, 按下鼠标不松开
    5. :focus ----- 获取焦点的元素, 表单类点击, 触碰, 或使用 TAB, 正在输入
* 伪类选择器 -- 结构伪类
    1. :first-child ----- 所有兄弟元素中的第一个
    2. :last-child ------ 所有兄弟元素中的最后一个
    3. :nth-child(n) ---- 所有兄弟元素中的第 n 个, an+b
    4. :first-of-type --- 所有同类型兄弟元素中的第一个
    5. :last-of-type ---- 所有同类型兄弟元素中的最后一个
    6. :nth-of-type(n) -- 所有同类型兄弟元素中的 第n个
* 伪类选择器 -- 否定伪类
    1. not()
* 伪类选择器 -- UI伪类
    1. :checked ---- 被选中
    2. :enable ----- 可用的
    3. :disabled --- 不可用
* 伪类选择器 -- 目标伪类
    1. target
* 伪类选择器 -- 语言伪类
    1. lang
* 伪元素选择器
    * ::first-letter ----- 选中元素中的第一个文字
    * ::first-line ------- 选中元素中的第一行文字
    * ::selection -------- 选中被鼠标选中的内容
    * ::placeholder ------ 选中输入框的提示文字
    * ::before ----------- 在元素最开始的位置，创建一个子元素（必须用 content 属性指定内容）
    * ::after ------------ 在元素最后的位置，创建一个子元素（必须用 content 属性指定内容）

## 像素
* 单个像素点越小, 显示越清楚
* 屏幕: 4K 屏幕指的是屏幕上实际有多少像素点, 实际的显示受分辨率控制, 和屏幕的大小没有必然的关系
* 图片: 1920*1080 指的是照片上实际有多少像素点, 和图片的大小没有必然的关系
* 视频: 1080P 指的是每一帧上实际有多少像素点, 和视频的大小没有必然的关系
* 文字: 指实际有多少像素点, 和文字的大小没有必然的关系

## 单位
* 毫米
* 厘米
* 像素
* 倍数 ---- 相当于文字大小的倍数
* 百分比 -- 相对于文字大小的百分比
* vw ------ 相对于视口宽度的百分比
* vh ------ 相对于视口高度的百分比

## 颜色
* 具体颜色
* RGB: 红绿蓝
* RGBA: 红绿蓝 透明度
* HEX: 十六进制红绿蓝
* HEXA: 十六进制红绿蓝 透明度
* HSL: 色相 饱和度 亮度
* HSLA: 色相 饱和度 亮度 透明度

## 盒子模型
1. margin ---------- 外边距, 盒子与外界的距离
    * 上和左外边距影响自身的位置, 下和右外边距影响后面兄弟的位置
    * 行内元素的左右外边距没问题, 上下内边距不能设置
    * 块元素和行内块元素可以完美设置四个外边距
    * 块级元素左右外边距设置为 auto, 可以水平居中
    * 第一个元素的上外边距和最后一个元素的下外边距会作用在父元素上
        1. 父元素设置不为 0 的 padding
        2. 父元素设置不为 0 的 border
        3. 父元素设置 overflow:hidden
    * 上面兄弟的下边距和上面兄弟的上边距会合并, 取最大值, 修改: 只设置一个外边距
2. border ---------- 边框, 盒子的边框
3. padding --------- 内边距, 紧贴内容的补白区域
    * 行内元素的左右内边距没问题, 上下内边距不能完美设置
    * 块元素和行内块元素可以完美设置四个内边距
4. content --------- 内容, 元素中的文本或后代元素都是它的内容
    * 行内元素无法修改的宽和高
    * 块元素和行内块元素可以修改的宽和高

## 布局总结
* 水平对齐 ----- 指内容在父元素的内容区的对齐方式
    * text-align 只能配置在块元素, 不能配置在行内元素或行内块元素
    * 如果内容为文本, 父元素直接使用 text-align
    * 如果内容为行内元素, 父元素直接使用 text-align
    * 如果内容为行内块元素, 父元素直接使用 text-align
    * 如果内容为块元素
        * 左对齐: 直接在该块元素内设置, 右外边距为 auto, 默认
        * 居中对齐: 直接在该块元素内设置, 左右外边距为 auto
        * 右对齐: 直接在该块元素内设置, 左外边距为 auto
*  该元素在当前行的垂直对齐 vertical-align, 基线对齐, 顶部对齐, 中部对齐, 底部对齐
    * 在父标签上配置
    * 包括文本, 单元格, 以及其他行内元素和行内块元素
    * 加上 `font-size:0` 表示绝对居中, 不受字体大小的影响
* 垂直对齐 ----- 指内容在父元素的内容区的对齐方式
    * height 和 line-height 不能写在行内元素
    * 单行文本对齐
        * 顶部对齐: 默认
        * 中部对齐:  height = line-height
        * 底部对齐: 通过定位去做
    * 多行文本对齐
        * 顶部对齐: 默认
        * 中部对齐: 通过定位去做
        * 底部对齐: 通过定位去做
    * 块元素
        * 顶部对齐: 默认
        * 居中对齐: 设置 margin-top, (父元素内容区的高度-  子元素的高度) / 2
        * 底部对齐: 通过定位
    * 行内元素-行内块元素
        * 顶部对齐: 默认
        * 居中对齐: 设置: 父元素, height = line-height, 每个子元素, `verticalalign:middle;`
        * 底部对齐: 通过定位
* 行内元素和行内块元素可以被父元素当作单行的文本处理
* 元素之间的空白, 父元素设置: `font-size:0;`
* 行内块的幽灵空白: 基线对齐, 基线和底部有一定的空白
    1. 设置行内块的对齐方式
    2. display 修改为 block
    3. 设置父元素的 `font-size:0`

## 浮动
* 会脱离文档流
* 浮动后的任何元素的宽和高都由内容撑开
* 浮动后的任何元素都可以设置宽和高
* 浮动后的任何元素都和其他元素公用一行
* 没有任何的 margin 和 padding 的问题
* 四个方向的 margin 和 padding 都可以完美设置
* 不会有行内空白的问题
* 会对后面的兄弟元素有影响
* 父元素需要清除浮动

## 定位
* 相对定位
    * 相对于一开始的位置
    * 不会脱离文档流
    * 对其他元素的位置没影响, 可能会覆盖其他元素的值
* 绝对定位
    * 相对于包含块(第一个拥有定位属性的父元素)
    * 会脱离文档流
    * 所有元素宽高都被内容撑开, 都能设置宽高
* 固定定位
    * 相对于视口
    * 会脱离文档流
    * 所有元素宽高都被内容撑开, 都能设置宽高
* 粘性定位





* 定位元素使用的优先级高


## 伸缩盒模型
* display:flex ----- 开启伸缩盒模型, 子元素变为伸缩元素, 子元素块状化
* flex-direction --- 主轴排列方向, 左右, 上下
* flex-wrap -------- 主轴布满后的换行方式
* flex-flow -------- 复合属性, 一般不用
* justify-content -- 主轴的对齐方式: 起点对齐, 终点对齐, 居中对齐, 均匀对齐
* align-items ------ 侧轴的对齐方式(单行)
* align-content ---- 侧轴的对齐方式(多行)
* flex-basis ------- 设置主轴的基准长度, -- 不常用
* flex-grow -------- 主轴有剩余空间时, 是否放大
* flex-shrink ------ 主轴空间不够时, 是否缩小
* flex ------------- 上三个属性的复合
* order ------------ 设置排序的顺序


## 常见属性
* font-size ------------ 字体, 字体大小, 浏览器有最小限制
* font-family ---------- 字体, 字体类型
* font-style ----------- 字体, 字体风格, 正常, 斜体
* font-weight ---------- 字体, 字体粗细, 细, 正常, 粗, 可以数字
* font ----------------- 字体, 复合写, 大小和族必须放到最后两位
* color ---------------- 文本, 颜色
* letter-spacing ------- 文本, 字母间距, 包括汉字, 可以是负值
* word-spacing --------- 文本, 单词间距(通过空格区分), 可以是负值
* text-decoration ------ 文本, 修饰符, 无, 上划线, 下划线, 删除线, 虚线, 波浪线, 颜色
* text-indent ---------- 文本, 首行缩进
* text-align ----------- 文本, 子元素在内容区的水平对齐方式
* line-height ---------- 文本, 行高, 可以控制行间距, normal, 像素, 倍数, 百分比相对于 font-size
    * 行在内容区的对齐方式, 默认顶部对齐
    * 单行文本, 如果 line-height 等于 height, 居中对齐
    * 多行文本, TODO
* vertical-align ------- 文本, 该元素在当前行的垂直对齐方式, 默认是顶部对齐, 不能处理块元素
* list-style-type ------ 列表, 设置列表符号
* list-style-position -- 列表, 设置列表符号的位置
* list-style-image ----- 列表, 自定义列表符号 url(图片地址)
* list-style ----------- 列表, 复合属性
* table-layout --------- 表格, 设置列宽度, 自适应, 还是等宽
* border-spacing ------- 表格, 单元格间距
* border-collapse ------ 表格, 合并单元格边框
* empty-cells ---------- 表格, 隐藏没有内容的单元
* caption-side --------- 表格, 设置表格标题位置
* background-color ----- 背景, 设置背景颜色
* background-image ----- 背景, 设置背景图片
* background-repeat ---- 背景, 设置背景重复方式
* background-position -- 背景, 设置背景图位置
* background ----------- 背景, 复合属性
* cursor --------------- 设置鼠标光标的样式
* display -------------- 隐藏, 或 转换元素类型
* width ---------------- 盒子内容区, 宽度
* max-width ------------ 盒子内容区, 最大宽度
* min-width ------------ 盒子内容区, 最小宽度
* height --------------- 盒子内容区, 高度
* max-height ----------- 盒子内容区, 最大高度
* min-height ----------- 盒子内容区, 最小高度
* padding-top ---------- 盒子内边距, 上内边距
* padding-right -------- 盒子内边距, 右内边距
* padding-bottom ------- 盒子内边距, 下内边距
* padding-left --------- 盒子内边距, 左内边
* padding -------------- 盒子内边距, 复合属性
* border-width --------- 盒子边框, 边框宽度
* border-color --------- 盒子边框, 边框颜色
* border-style --------- 盒子边框, 边框风格, none, 实线, 虚线, 点线, 双实线
* border --------------- 盒子边框, 边框复合属性
* border-left ---------- 盒子左边框, 同上
* border-left-style
* border-left-width
* border-left-color
* border-right --------- 盒子右边框, 同上
* border-right-style
* border-right-width
* border-right-color
* border-top ----------- 盒子上边框, 同上
* border-top-style
* border-top-width
* border-top-color
* border-bottom -------- 盒子下边框, 同上
* border-bottom-style
* border-bottom-width
* border-bottom-color
* margin-left ---------- 盒子外边距, 左外边距, 给块元素设置左右 auto , 可以水平居中
* margin-right --------- 盒子外边距, 右外边距
* margin-top ----------- 盒子外边距, 上外边距
* margin-bottom -------- 盒子外边距, 下外边距
* margin --------------- 盒子外边距, 复合属性
* overflow ------------- 溢出内容的处理方式, 显示, 隐藏, 滚动条
* overflow-x ----------- 水平方向溢出内容的处理方式
* overflow-y ----------- 垂直方向溢出内容的处理方式
* visibility ----------- 隐藏元素, 但位置还占着
* display -------------- 隐藏元素, 彻底隐藏
