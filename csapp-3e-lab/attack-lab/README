**************************
 csapp-3e-lab-attcak-lab
**************************

本实验主要是缓冲区溢出实验，用于模拟攻击行为

实验官网：http://csapp.cs.cmu.edu/3e/labs.html

***********************
实验一：
***********************

直接利用溢出，将返回地址改为 touch1 即可

***********************
实验二：
***********************

直接利用溢出，将返回地址改为另一地址，在该地址，将 cookie 打入参数，
然后，将函数的返回地址设为 touch2 即可

***********************
实验三：
***********************

直接利用溢出，将返回地址改为另一地址，在该地址，将 cookie 的字符串写入自己的栈空间，
将指向字符串的指针打入参数，由于后续的函数会申请栈空间，所以，要调整栈指针，避免数据被破坏，
最后，将函数的返回地址设为 touch3 即可


接下来两个实验，采用了栈随机化，和 栈不可执行，因此，不能准确获得栈的地址，
以及，在栈上执行代码

***********************
实验四：
***********************

和实验二类似，只是不能跳到栈地址，以及不能在栈上执行代码，需要找到代码片段，达到目的

***********************
实验五：
***********************

和实验三类似，不过查找代码片段太麻烦了，直接理解的答案

总结：
* 千万不能缓冲区溢出，不要使用 gets

