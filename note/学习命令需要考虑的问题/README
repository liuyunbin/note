
## 学习命令需要考虑的问题
* 涉及过滤时, 多个命令是且还是或
* 使用的是通配符, 基础的正则表达式 还是扩展的正则表达式
* 对符号链接的处理, 这个可以先猜, 有利于加深理解
* 对转义字符的处理
* 对于一般用不到的命令, 只需知道大致功能即可, 比如, 组相关的命令
* 对于比较复杂的命令, 需要总结出常用的参数, 每次都过一遍 man 文档太麻烦了, 比如, ps
* 切记只需了解常用参数即可, 其他参数用到再说, 有的参数估计一辈子也不会使用到, 切记切记
* 学习命令要以功能为核心而不是以命令参数为核心
* 分隔符支持多个吗? 对于转义字符是如何处理的
* 不要垃圾进垃圾出, 宁可不写, 也不写错的, 或者 不懂的内容

