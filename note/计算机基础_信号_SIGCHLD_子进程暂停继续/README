
## 信号 SIGCHLD 和子进程状态变化(不包括子进程退出)

## 子进程状态变化
* 子进程暂停(调用 pause() 或 收到信号 SIGSTOP SIGTSTP)
* 子进程继续(收到信号 SIGCONT ...)
* 后台进程读终端(收到信号 SIGTTIN)
* 后台进程写终端(收到信号 SIGTTOU)
## 信号 SIGCHLD 和子进程状态变化(不包括子进程退出)

## 其他类似信号
* 空指针    会触发的 SIGSEGV, 如果忽略 或者 捕获后从信号处理函数返回, 将反复触发
* 整数除以零会触发的 SIGFPE,  如果忽略 或者 捕获后从信号处理函数返回, 将反复触发

