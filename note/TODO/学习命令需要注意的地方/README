
## 学习命令需要考虑的问题
* 使用的是通配符, 基础的正则表达式 还是扩展的正则表达式
* 如何处理符号链接
* 多个命令之间是且还是或
* 对转义字符的处理
* 学习命令要以功能为核心而不是以命令参数为核心
* 切记只需了解常用参数即可, 其他参数用到再说, 有的参数估计一辈子也不会使用到, 切记切记

## 命令的掌握程度
1. 对于一般用不到的命令, 只需知道大致功能即可, 比如, 组相关的命令
2. 对于比较复杂的命令, 需要总结出常用的参数, 每次都过一遍 man 文档太麻烦了, 比如, ps
3. 对于一些命令, 只需知道特殊参数对应的功能即可, 比如, uptime -s 查看开机时间
4. 对于常用的命令, 需要大致知道常用参数的作用, 到时候使用 man 查即可

