
## 孤儿进程
子进程未退出, 父进程已退出的情况

## 孤儿进程组
该进程组内所有进程的父进程, 要么属于该进程组, 要么不属于此会话

## 孤儿进程产生的场景
1. fork() 产生子进程
2. 子进程空循环
3. 父进程退出, 此时子进程是孤儿进程

## 后续处理
1. 孤儿进程的父进程将变为 systemd
2. 如果该进程属于孤儿进程组且组内有进程处于暂停状态
    * systemd 将发送 SIGCONT 和 SIGHUP 给该孤儿进程组的每一个成员

## 孤儿进程存在的意义
1. 保证所有进程退出的资源都能被释放
2. 建立脱离终端的服务时会用到

