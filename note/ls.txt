
#### ls 常用命令
* ls        # 列出当前目录中的元素
* ls -a     # 列出当前目录中的元素（包括隐藏的文件）
* ls -l     # 列出当前目录中的元素的详细信息
* ls -F     # 在目录后添加 /，在可执行文件后添加 *
* ls -d     # 只列出目录本身，而不列出目录内元素
* ls -r     # 逆序列出当前目录中的元素
* ls -R     # 递归列出当前目录中的元素
* ls -1     # 在每一行列出文件名

