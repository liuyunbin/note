
######################################################
#                      简介
######################################################

通过实现部分 linux 命令来学习 linux 命令

lyb=123           # 定义变量
echo $lyb         # 输出变量的值
echo ${lyb}       # 输出变量的值
lyb=(1 2 3)       # 定义数组
echo ${lyb[1]}    # 输出数组中指定元素的值
echo ${lyb[*]}    # 输出数组中所有元素的值
lyb=$[1+3]        # 定义变量，包括数学运算
ls -l `which ls`  # 命令替换，首先执行 which ls
ls -l $(which ls) # 命令替换，首先执行 which ls

IFS=$'\n' # 将字段分割符改成 '\n'
IFS=:     # 将字段分割符改成 :

#####################################################
#                    参考资源
#####################################################

* unix/linux 编程实践教程

