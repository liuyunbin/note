
on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - run: ./src/bash/install-dependency.sh
      - run: ./src/bash/build.sh
