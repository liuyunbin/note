
## 视图
```
* 相当于是一张虚拟表
* 不存储数据
* 一般只用于查询
* 简化操作, 便于操作
* 提高数据安全
* 底层变化时, 必须更新视图
create [or replace] view view_name as select ... # 创建或更新视图
alter view 视图名称 as select ...                # 更新视图
drop view view_name;                             # 删除视图
```

